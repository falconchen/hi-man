<ul class="w3-ul  w3-mobile ">
        {%for post in posts %}            
            
            <li id="post-{{ post.post_name }}" class="w3-border-0 {% if r == 'homepage.tweet' %} w3-margin-top w3-margin-bottom{% endif %}">
            {% block postTypeItem %}             
                {% include "post-type/" ~ post.post_type ~".twig" %}                
            {% endblock %} 
            </li>
        {% endfor %}
    </ul>
    <div class=" w3-center w3-margin-top">
    {% include 'pagination-w3css.twig' with {data: posts} only%}
    </div>